import React from "react";

const ThemeContext = React.createContext();

// Set name of the Context for easier debugging in DevTools
ThemeContext.displayName = 'ThemeContext';

export default ThemeContext;