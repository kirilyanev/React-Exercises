function Header() {
    let headingMessage = 'Hello React World from custom component';

    return (
        <header>
            <h1>{headingMessage}</h1>
            <h2>The Best Framework Ever</h2>
        </header>
    );
}

export default Header;